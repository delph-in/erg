;;; redefine relation adding cfrom and cto
;;; for characterization experiments

initial-cfrom-val := atom.

initial-cto-val := atom.

string := initial-cfrom-val & initial-cto-val.

relation := *avm* &
  [ LBL handle,
    WLINK *list*,
    PRED predsort,
    CFROM *top*,
    CTO *top* ].

