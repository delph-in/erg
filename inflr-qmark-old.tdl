; Unfortunately, the new LKB code requires a double backslash before the 
; punctuation mark ?, while the previous LKB version only allows one
; backslash (or none).  So we have two different version of this tiny file.

punct_qmark_rule :=
%suffix (!? !?\?)
punctuation_qmark_rule.
